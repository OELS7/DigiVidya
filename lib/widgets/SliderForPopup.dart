import 'package:flutter/material.dart';

class sliderForPopup extends StatefulWidget {
  const sliderForPopup({super.key});

  @override
  State<sliderForPopup> createState() => _sliderForPopupState();
}

class _sliderForPopupState extends State<sliderForPopup> {
  @override
  Widget build(BuildContext context) {
    return AlertDialog();
  }
}
